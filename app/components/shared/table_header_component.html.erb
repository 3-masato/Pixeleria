<thead class="bg-gray-200">
  <tr>
    <% @headers.each do |header| %>
      <th scope="col" class="px-4 py-2 text-start whitespace-nowrap">
        <span class="text-sm font-semibold tracking-wide text-gray-800">
          <%= header %>
        </span>
      </th>
    <% end %>
  </tr>
</thead>
