{
  const commentList = $("#comment-list-of-<%= @artwork.id %>");
  commentList.html("<%= j(render "public/comments/list", comments: @artwork.comments) %>");

  const commentCount = $("#comment-count-<%= @artwork.id %>");
  commentCount.html("<%= j(render Interaction::CommentCountComponent.new(count: @artwork_comments.size)) %>");
}