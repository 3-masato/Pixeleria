{
  const closeButton = document.getElementById("modal-close-button");
  if (closeButton) {
    closeButton.dispatchEvent(new Event("click"));
  }

  document.body.insertAdjacentHTML("beforeend", "<%= j(component "layout/alert", flash: flash) %>");
}