{
  const artworks = document.getElementById("artworks");
  if (artworks) {
    artworks.innerHTML = "<%= j(
      render(
        "public/users/artworks",
        artworks: @user_liked_artworks,
        more_link: user_liked_artworks_path(@user.account_name),
        not_found_text: t("messages.user.artworks.no_liked_artworks")
      )
    ) %>";
  }

  const artworksButton = document.getElementById("artworks-button");
  const likedArtworksButton = document.getElementById("liked-artworks-button");
  if (artworksButton && likedArtworksButton) {
    artworksButton.classList.add("border-transparent");
    artworksButton.classList.remove("border-sky-400");
    artworksButton.classList.remove("pointer-events-none");
    likedArtworksButton.classList.remove("border-transparent");
    likedArtworksButton.classList.add("border-sky-400");
    likedArtworksButton.classList.add("pointer-events-none");
  }
}
