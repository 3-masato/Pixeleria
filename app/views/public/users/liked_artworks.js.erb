{
  const artworks = document.getElementById("artworks");
  if (artworks) {
    artworks.innerHTML = "<%= j(
      render(
        "public/users/artworks",
        artworks: @user_liked_artworks,
        more_link: user_liked_artworks_path(@user.account_name),
        not_found_text: t("messages.user.artworks.no_liked_artworks")
      )
    ) %>";
  }
}
