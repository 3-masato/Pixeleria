{
  const artworks = document.getElementById("artworks");
  if (artworks) {
    artworks.innerHTML = "<%= j(
      render(
        "public/users/artworks",
        artworks: @user_artworks,
        more_link: user_artworks_path(@user.account_name),
        not_found_text: t("messages.user.artworks.no_artworks")
      )
    ) %>";
  }

  const artworksButton = document.getElementById("artworks-button");
  const likedArtworksButton = document.getElementById("liked-artworks-button");
  if (artworksButton && likedArtworksButton) {
    artworksButton.classList.remove("border-transparent");
    artworksButton.classList.add("border-sky-400");
    artworksButton.classList.add("pointer-events-none");
    likedArtworksButton.classList.add("border-transparent");
    likedArtworksButton.classList.remove("border-sky-400");
    likedArtworksButton.classList.remove("pointer-events-none");
  }
}
